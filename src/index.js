import React from 'react';
import ReactDOM from 'react-dom';
import GameBoard from './js/Game';
import 'normalize.css/normalize.css'

ReactDOM.render(
  <GameBoard/>,
  document.getElementById('root')
);